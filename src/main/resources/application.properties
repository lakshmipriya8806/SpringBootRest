# create and drop tables and sequences, loads import.sql

spring.jpa.hibernate.ddl-auto=update

# MYSQL settings

spring.datasource.url=jdbc:mysql://localhost:3306/test

spring.datasource.username=root

spring.datasource.password=admin

spring.datasource.driver.class=com.mysql.jdbc.Driver

# Spring Actuator

info.app.name=Priya's SpringBoot Application
info.app.description=Spring boot application with Hibernate and MySQL
info.app.version=1.0.0

# Changing the port of the server to 9090 from 8080
#server.port=8080

# Changing the context when running as Spring Boot Application
server.servlet.context-path=/SpringBootRest

